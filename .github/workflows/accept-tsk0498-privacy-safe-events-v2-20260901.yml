name: Temporary - TSK-0361 GREEN acceptance contract

on:
  push:
    branches:
      - feature/tsk-0361-nextjs-public-site-20260902
    paths:
      - '.github/workflows/accept-tsk0498-privacy-safe-events-v2-20260901.yml'

permissions:
  contents: read

jobs:
  green-tsk0361:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22.23.2'
      - name: Run focused TSK-0361 contract test
        shell: bash
        run: |
          set -euo pipefail
          node --test website/tests/contract/tsk0361.test.mjs
