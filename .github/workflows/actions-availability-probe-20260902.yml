name: Actions availability probe 2026-09-02

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/actions-availability-probe-20260902.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Prove job scheduling
        run: echo ACTIONS_JOB_SCHEDULING=PASS
