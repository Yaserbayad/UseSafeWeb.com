name: Inspect current TSK-0331
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/inspect-tsk0331-current-20260831.yml'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Inspect exact current TSK-0331
        shell: bash
        run: |
          set -euo pipefail
          test "$(hostname -s)" = "adguardvm"
          python3 .github/scripts/inspect_tsk0331_current_20260831.py
          git diff --check
          test -z "$(git status --porcelain)"
