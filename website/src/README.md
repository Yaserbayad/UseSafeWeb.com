# Website source

This directory is reserved for the TSK-0361-owned TypeScript/Next.js application source and its downstream feature slices.

Create concrete application directories (`app`, components, server modules, utilities, localization, and similar) only when their owning implementation task requires them. Keeping TSK-0454 structural avoids inventing framework conventions or abstractions before implementation authority reaches those tasks.
