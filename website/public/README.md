# Website public assets

Version-controlled assets that are intentionally public at runtime belong here when an owning implementation/content task publishes them.

Do not place credentials, private keys, internal evidence, raw DNS data, participant data or environment-specific configuration in this directory. Generated optimization output remains build output rather than source unless an explicit acceptance contract requires otherwise.
