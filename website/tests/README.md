# Website test root

Application-owned unit, integration, accessibility and browser/E2E tests live here unless the implemented framework's approved conventions require a more specific colocated location.

Cross-repository governance/structure checks stay under `/tests`; test evidence is produced by CI and referenced durably when a task's acceptance contract requires it. Generated coverage reports are not committed by default.
