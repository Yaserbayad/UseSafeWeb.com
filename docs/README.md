# Implementation documentation

`/docs` is reserved for durable **non-authoritative** implementation/developer documentation that does not naturally belong with `/website` or `/infrastructure`.

Project planning, task definitions, dependencies, requirements, gates, risks, decisions and authority routing remain under `Plans/Master/`; volatile runtime state remains in `CURRENT_STATE.md`. Documentation here may explain implemented behavior but must not duplicate or override those authorities.
